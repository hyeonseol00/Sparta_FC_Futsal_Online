/*import { handleWin, handleLose, handleDraw } from './logics/score.logic.js';

const main = async () => {
  try {
    const winResult = await handleWin('jiwon2', 'userIdB', 1, 2);
    console.log(winResult);

    const loseResult = await handleLose('jiwon2', 'userIdB', 1, 2);
    console.log(loseResult);

    const drawResult = await handleDraw('jiwon2', 'userIdB', 1, 2);
    console.log(drawResult);
  } catch (error) {
    console.error('Error:', error.message);
  }
};

main(); */
